# Required for Node.js 20.18 compatibility with Angular 21 + ESM modules
node-options=--experimental-require-module

# Hoist packages to avoid duplicate module instances with Angular + Vite
node-linker=hoisted
